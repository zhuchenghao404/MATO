- 采用组合式 API (Composition API) 与 `<script setup>` 语法糖编写 Vue 组件。
- 使用 SCSS 嵌套样式并结合 scoped 作用域，部分全局 UI 壳层通过非 scoped 样式配合 Teleport 实现。
- 通过自定义 `request` 封装函数统一处理 API 请求的 Header（如 Authorization）与 JSON 序列化。